import groovy.transform.*
import org.olf.erm.OrderLine

@Field OrderLine orderLine

inherits template: "/remoteOkapiLink/remoteOkapiLink"

if (params.controller == 'orderLine' ) {
  should_expand << 'owner'
}

json g.render(orderLine, ['excludes': ['id', 'version'], 'expand':should_expand])
