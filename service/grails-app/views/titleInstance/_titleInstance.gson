import org.olf.kb.TitleInstance

import groovy.transform.*

@Field
TitleInstance titleInstance

json g.render(titleInstance, [excludes: ['name'], expand: ['identifiers', 'type', 'subType', 'coverage'], excludes: ['entitlements']]) {
  name titleInstance.longName
  shortName titleInstance.name
}